import { boolean, index, pgTable, text, timestamp, uniqueIndex, uuid } from "drizzle-orm/pg-core";
import { researchReviewKindEnum, researchStatusEnum } from "./enums";
import { organisations } from "./organisations";
import { users } from "./auth";

export const researchProposals = pgTable("research_proposals", { id: uuid("id").primaryKey().defaultRandom(), organisationId: uuid("organisation_id").notNull().references(() => organisations.id, { onDelete: "cascade" }), key: text("key").notNull(), title: text("title").notNull(), hypothesis: text("hypothesis").notNull(), exitCriteria: text("exit_criteria").notNull(), dataProtectionPlan: text("data_protection_plan").notNull(), status: researchStatusEnum("status").notNull().default("draft"), ownerUserId: uuid("owner_user_id").notNull().references(() => users.id), outcome: text("outcome"), outcomeUrl: text("outcome_url"), rejectionRationale: text("rejection_rationale"), promotedIssueUrl: text("promoted_issue_url"), createdAt: timestamp("created_at", { withTimezone: true }).notNull().defaultNow(), updatedAt: timestamp("updated_at", { withTimezone: true }).notNull().defaultNow() }, (t) => [uniqueIndex("research_proposal_key_idx").on(t.organisationId, t.key), index("research_proposal_status_idx").on(t.organisationId, t.status)]);

export const researchReviews = pgTable("research_reviews", { id: uuid("id").primaryKey().defaultRandom(), proposalId: uuid("proposal_id").notNull().references(() => researchProposals.id, { onDelete: "cascade" }), kind: researchReviewKindEnum("kind").notNull(), approved: boolean("approved").notNull(), notes: text("notes").notNull(), reviewedByUserId: uuid("reviewed_by_user_id").notNull().references(() => users.id), createdAt: timestamp("created_at", { withTimezone: true }).notNull().defaultNow() }, (t) => [index("research_review_proposal_idx").on(t.proposalId, t.kind, t.createdAt)]);
