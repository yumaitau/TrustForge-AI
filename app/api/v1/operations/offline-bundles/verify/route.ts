import{NextResponse}from"next/server";import{z}from"zod";import{verifyOfflineBundle}from"@/lib/compliance/service";import{apiError}from"@/lib/http/responses";export async function POST(r:Request){try{const p=z.object({version:z.string(),manifest:z.string().max(5_000_000),signature:z.string(),secret:z.string().min(32)}).parse(await r.json());return NextResponse.json({data:await verifyOfflineBundle(p)})}catch(e){return apiError(e)}}
